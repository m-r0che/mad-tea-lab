# Refuse to install any package version published less than 7 days ago.
# Supply-chain hygiene: gives security advisories a chance to surface
# before a freshly-published malicious release ends up in node_modules.
# Requires npm >= 11.10 (Feb 2026). Value is in days.
min-release-age=7
